import styled from '@emotion/styled'

const StyledSection = styled.div`
  width: 100%;
  height: 100%;
  background-color: violet;
`

export default function Detail() {
  return <StyledSection>dd</StyledSection>
}
